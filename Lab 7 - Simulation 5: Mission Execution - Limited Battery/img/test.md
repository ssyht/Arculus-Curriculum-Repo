# Git push test

## Basic Commands include: 

```bash
git status

```

To check the status of the updates

```bash
git add . 
```

To set up the change.

```bash
git commit -m "Comment"
```

This basically commits it with a message of what is being pushed (under the comments in the command line)

```bash
git push origin

```

Final push to the repository. 